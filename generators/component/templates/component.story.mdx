<!-- <%= name %>.story.mdx -->

import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { <%= name %> } from '../<%= name %>';

<Meta title="<%= project %>/<%= name %>" component={<%= name %>} />


export const Template = (args) => <<%= name %> {...args } />

# <%= name %>

This is how to use `<%= name %>` component:

<Canvas>
  <Story 
    name="default"
    args={{
      
    }}>
    {Template.bind({})}
  </Story>
</Canvas>