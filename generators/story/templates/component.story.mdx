<!-- <%= name %>.story.mdx -->

import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { <%= componentName %> } from '<%= relativePath %>';

<Meta title="<%= project %>/<%= componentName %>" component={<%= componentName %>} />


export const Template = (args) => <<%= componentName %> {...args } />

# <%= componentName %>

This is how to use `<%= componentName %>` component:

<Canvas>
  <Story 
    name="default"
    args={{
      
    }}>
    {Template.bind({})}
  </Story>
</Canvas>